#include<stdio.h>
main()
{
    int n,i,arr[10],x;
    printf("Enter the size of the array: ");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    x=arr[0];
    for(i=0;i<n;i++)
    {
        if(x<arr[i])
            x=arr[i];
    }
    printf("%d",x);
}
