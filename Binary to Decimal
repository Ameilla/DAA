#include<stdio.h>
main()
{
    int n,dec=0,rem,i=0;
    printf("Enter the Binary Number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        rem=n%10;
        dec=dec+rem*pow(2,i);
        i++;
        n=n/10;
    }
    printf("Decimal Number is %d ",dec);
}
